# Experiment log

Copy this entry for each planned or executed run. Use `TBD` for unknown values.

## <run ID> — <date> — <planned/running/completed/failed/stopped>
- Question / hypothesis:
- Method / baseline / ablation:
- Commit / dirty changes / config / command:
- Data version / QC version / split manifest and checksum:
- Environment / hardware / seed(s) / compute budget:
- Evaluation protocol / metrics:
- Artifact locations / checksums:
- Results per seed / aggregate / uncertainty:
- Failures / deviations / interpretation:
- Related decision / review / next step:
