method: jepa
model: {patch_size: 50, patch_stride: 50, embed_dim: 128, depth: 4, num_heads: 4, mlp_ratio: 4.0, dropout: 0.0, decoder_dim: 64, decoder_depth: 2, decoder_num_heads: 4, decoder_mlp_ratio: 4.0}
data: {manifest: data/processed/manifest.csv, processed_root: data/processed, input_length: 5000, expected_preprocessing_version: ppg_preprocessing_v0}
train: {batch_size: 16, epochs: 1, max_updates: 10, learning_rate: 0.0001, weight_decay: 0.05, seed: 0, amp: false, num_workers: 0, drop_last: true, run_dir: runs/jepa}
jepa: {num_target_blocks: 2, target_block_length: 4, predictor_dim: 128, predictor_depth: 2, predictor_num_heads: 4, predictor_mlp_ratio: 4.0, ema_momentum: 0.996, loss_beta: 1.0}
